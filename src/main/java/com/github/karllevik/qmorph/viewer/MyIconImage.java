package com.github.karllevik.qmorph.viewer;

/**
 * Unfinished class for creating the icon to display whenever the application is
 * minimized.
 */

public class MyIconImage {
	/*
	 * public static Image makeIconImage() { HashTable hash= null;
	 * 
	 * 
	 * IndexColorModel cm= new IndexColorModel(int bits, int size, byte[] r, byte[]
	 * g, byte[] b); cm.createCompatibleSampleModel(30, 30); WritableRaster raster=
	 * cm.createCompatibleWritableRaster(30, 30); DataBufferByte db= new
	 * DataBufferByte(30*30); BufferedImage img= new BufferedImage(cm, raster,
	 * false, hash); return img; }
	 */
}
