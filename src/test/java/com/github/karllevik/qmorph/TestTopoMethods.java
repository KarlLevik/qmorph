package com.github.karllevik.qmorph;

import com.github.karllevik.qmorph.*;

class TestTopoMethods extends GeomBasics {

    public static void main(String[] args) {
	Msg.debug("Entering TestTopoMethods.main(..)");

	/*
	byte [] pat1= {10, 4, 4, 4, 4, 4, 0, 0, 0, 0};

	byte [] pat2= {10, 4, 4, 4, 4, 0, 0, 0, 0, 0};
	byte [] pat3= {10, 4, 0, 4, 4, 4, 4, 0, 0, 0};
	
	byte [] pat4= {10, 4, 4, 4, 4, 3, 5, 4, 4, 4};
	byte [] pat5= {10, 4, 4, 4, 4, 4, 3, 5, 4, 4};
	byte [] pat6= {10, 4, 1, 4, 4, 4, 3, 5, 4, 4};

	if (patternMatch(pat1, pat2))
	    Msg.debug("... pat1 match pat2");
	else
	    Msg.debug("... pat1 don't match pat2");
	Msg.debug("... -----------------------------");
	Msg.debug("");

	if (patternMatch(pat1, pat3))
	    Msg.debug("... pat1 match pat3");
	else
	    Msg.debug("... pat1 don't match pat3");
	Msg.debug("... -----------------------------");
	Msg.debug("");

	if (patternMatch(pat4, pat5))
	    Msg.debug("... pat4 match pat5");
	else
	    Msg.debug("... pat4 don't match pat5");
	Msg.debug("... -----------------------------");
	Msg.debug("");

	if (patternMatch(pat4, pat6))
	    Msg.debug("... pat4 match pat6");
	else
	    Msg.debug("... pat4 don't match pat6");
	Msg.debug("... -----------------------------");
	Msg.debug("");
	*/
	Msg.debug("Leaving TestTopoMethods.main(..)");
    }
}
